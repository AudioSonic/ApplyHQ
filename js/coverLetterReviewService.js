async function reviewCoverLetter(application, profile, fetcher = fetch) { if (!application?.coverLetter) throw new Error("Zuerst muss ein Anschreiben erstellt werden."); if (!application?.jobAnalysis || !application?.jobMatching) throw new Error("Stellenanalyse und Matching sind erforderlich."); const apiUrl = typeof window !== "undefined" ? window.APPLYHQ_JOB_SEARCH_API_URL || "" : ""; const contact = application.contact || {}; const response = await fetcher(`${apiUrl}/api/ai/cover-letter-review`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ coverLetter: application.coverLetter, jobAnalysis: application.jobAnalysis, jobMatching: application.jobMatching, profile: buildReviewProfile(profile), company: { name: application.company || "" }, position: application.position || "", contact: { salutation: contact.salutation || "", firstName: contact.firstName || "", lastName: contact.lastName || "" } }) }); let payload = {}; try { payload = await response.json(); } catch { /* handled below */ } if (!response.ok) throw new Error(payload.error || `Review-Service antwortete mit HTTP ${response.status}.`); if (!payload.success || !payload.review) throw new Error("Der Review-Service lieferte kein Ergebnis."); assertValidCoverLetterReview(payload.review); return finalizeCoverLetterReview(payload.review); }
