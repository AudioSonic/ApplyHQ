function openApplicationPreparation(application) { const modal = createModal(); modal.container.classList.add("application-workflow-container"); modal.title.textContent = `Bewerbung – ${application.company || "Unternehmen"}`; const profile = typeof getProfile === "function" ? getProfile() : null; const tabBar = document.createElement("nav"); tabBar.className = "workflow-tabs"; const panel = document.createElement("div"); panel.className = "workflow-panel"; const status = document.createElement("select"); status.className = "workflow-status"; ["open", "applied", "interview", "rejected", "accepted"].forEach(value => status.append(createOption(value, statusLabels[value] || value))); status.value = application.status || "open"; const date = createInput("workflow-date", "date", ""); date.value = application.date || ""; const metadata = document.createElement("aside"); metadata.className = "workflow-sidebar"; metadata.append(createLabel("workflow-status", "Status"), status, createLabel("workflow-date", "Bewerbungsdatum"), date); const tabs = [{ id: "analysis", label: "Stellenanalyse" }, { id: "matching", label: "Matching" }, { id: "cover", label: "Anschreiben" }]; let activeTab = application.coverLetter ? "cover" : application.jobMatching ? "matching" : "analysis"; const ready = tab => tab === "analysis" || (tab === "matching" ? Boolean(application.jobAnalysis) : Boolean(application.jobAnalysis && application.jobMatching)); const render = () => { panel.replaceChildren(); const area = document.createElement("div"); area.className = "workflow-main"; if (activeTab === "cover") area.append(metadata); area.append(createWorkflowTabContent(activeTab, application, profile, modal, render)); panel.append(area); tabBar.querySelectorAll("button").forEach(button => { button.classList.toggle("is-active", button.dataset.tab === activeTab); button.disabled = !ready(button.dataset.tab); }); }; tabs.forEach(tab => { const button = document.createElement("button"); button.type = "button"; button.textContent = tab.label; button.dataset.tab = tab.id; button.onclick = () => { if (ready(tab.id)) { activeTab = tab.id; render(); } }; tabBar.append(button); }); const save = document.createElement("button"); save.type = "button"; save.className = "button button-primary"; save.textContent = "Speichern"; save.onclick = () => { application.status = status.value; application.date = date.value; saveApplications(); renderApplications(); save.textContent = "Gespeichert"; setTimeout(() => save.textContent = "Speichern", 1200); }; const close = document.createElement("button"); close.type = "button"; close.className = "button"; close.textContent = "Schließen"; close.onclick = () => closeModal(modal.container); modal.footer.classList.add("workflow-footer"); modal.footer.append(save, close); modal.content.append(tabBar, panel); render(); }
function renderAnalysisTab(content, application, rerender) { const analysis = application.jobAnalysis; if (!analysis) { content.append(document.createTextNode("Die Stellenanzeige wurde noch nicht analysiert. Führe zunächst eine Stellenanalyse durch."), workflowAction("Stellenanzeige analysieren", async event => { event.currentTarget.disabled = true; const processing = beginAiProcess("Stellenanzeige wird analysiert", ["Aufgaben und Anforderungen werden erfasst …", "Technologien und Soft Skills werden strukturiert …", "Analyse wird validiert …"]); if (!processing) return; try { updateApplication(application, { jobAnalysis: await analyzeJobPosting(application) }); processing.close(); rerender(); } catch { processing.close(); alert("Die Stellenanzeige konnte nicht analysiert werden. Bitte versuche es erneut."); event.currentTarget.disabled = false; } })); return; } const title = document.createElement("h2"); title.textContent = analysis.position.title || application.position; content.append(title); workflowList(content, "Aufgaben", analysis.tasks); workflowList(content, "Muss-Anforderungen", analysis.requirements.mustHave); workflowList(content, "Wünschenswerte Anforderungen", analysis.requirements.niceToHave); workflowList(content, "Technologien", Object.values(analysis.skills).flat()); workflowList(content, "Soft Skills", analysis.softSkills); workflowList(content, "Benefits", analysis.benefits); content.append(workflowAction("Erneut analysieren", async event => { event.currentTarget.disabled = true; const processing = beginAiProcess("Stellenanzeige wird analysiert", ["Aufgaben und Anforderungen werden erfasst …", "Technologien und Soft Skills werden strukturiert …", "Analyse wird validiert …"]); if (!processing) return; try { updateApplication(application, { jobAnalysis: await analyzeJobPosting(application), jobMatching: null, coverLetter: null, coverLetterReview: null }); processing.close(); rerender(); } catch { processing.close(); alert("Die Stellenanzeige konnte nicht analysiert werden. Bitte versuche es erneut."); event.currentTarget.disabled = false; } })); }
function renderMatchingTab(content, application, profile, rerender) { if (!application.jobAnalysis) { content.append(document.createTextNode("Führe zunächst die Stellenanalyse durch.")); return; } if (!profile || !(profile.summary || profile.experience?.length || profile.education?.length || profile.projects?.length || Object.values(profile.skills || {}).some(items => items.length))) { content.append(document.createTextNode("Für das Matching ist ein ausgefülltes Bewerberprofil erforderlich.")); return; } const matching = application.jobMatching; if (!matching) { content.append(document.createTextNode("Für diese Stelle wurde noch kein Matching durchgeführt."), workflowAction("Matching durchführen", async event => { event.currentTarget.disabled = true; const processing = beginAiProcess("Profil-Matching läuft", ["Profil wird mit der Stelle abgeglichen …", "Muss-Anforderungen werden geprüft …", "Relevante Projekte werden ermittelt …", "Passungswert wird berechnet …"]); if (!processing) return; try { updateApplication(application, { jobMatching: await matchApplicationProfile(application, profile) }); processing.close(); rerender(); } catch { processing.close(); alert("Das Matching konnte nicht abgeschlossen werden. Bitte versuche es erneut."); event.currentTarget.disabled = false; } })); return; } const score = document.createElement("h2"); score.textContent = `${matching.score} %`; const rating = document.createElement("p"); rating.textContent = matching.rating; content.append(score, rating); workflowList(content, "Muss-Anforderungen", [`${matching.requirements.mustHave.matched}/${matching.requirements.mustHave.total} erfüllt`]); workflowList(content, "Technologien", [`${matching.skills.matched.length} vorhanden`]); workflowList(content, "Berufserfahrung", [matching.experience.rating || matching.experience.status]); workflowList(content, "Ausbildung", [matching.education.rating || matching.education.status]); workflowList(content, "Projekte", [matching.projects.rating || matching.projects.status]); workflowList(content, "Soft Skills", [matching.softSkills.rating || matching.softSkills.status]); content.append(workflowAction("Erneut matchen", async event => { event.currentTarget.disabled = true; const processing = beginAiProcess("Profil-Matching läuft", ["Profil wird mit der Stelle abgeglichen …", "Muss-Anforderungen werden geprüft …", "Relevante Projekte werden ermittelt …", "Passungswert wird berechnet …"]); if (!processing) return; try { updateApplication(application, { jobMatching: await matchApplicationProfile(application, profile, fetch, true), coverLetter: null, coverLetterReview: null }); processing.close(); rerender(); } catch { processing.close(); alert("Das Matching konnte nicht abgeschlossen werden. Bitte versuche es erneut."); event.currentTarget.disabled = false; } })); }
