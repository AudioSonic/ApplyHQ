name = "applyhq-job-search"
main = "src/index.js"
compatibility_date = "2026-08-23"

[vars]
ALLOWED_ORIGIN = "*"

# Optional: only set this as a Cloudflare secret when the provider requires it.
# wrangler secret put BA_API_KEY
